install_resolver_dependencies: true
install_tool_dependencies: false

tools:

# Text tools:

  - name: diff
    owner: bgruening

  - name: chatgpt_openai_api
    owner: bgruening

# Audio tools:

  - name: whisper
    owner: bgruening

# Imaging tools:
  

#Visualisation tools:

  - name: wordcloud
    owner: bgruening

