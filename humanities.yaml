install_resolver_dependencies: true
install_tool_dependencies: false

tools:

# Text tools:

  - name: diff
    owner: bgruening
    tool_panel_section_label: Text Manipulation

  - name: chatgpt_openai_api
    owner: bgruening
    tool_panel_section_label: Machine Learning

  - name: markitdown
    owner: bgruening
    tool_panel_section_label: "Convert Formats"
    
   # Stanford Core NLP
 
   # Editor
 
   # Regex / Grep

# Research Tools:

  - name: openalex_explorer
    owner: iuc
    tool_panel_section_label: Get Data


# Data Manipulation (tables, etc):

  # Open Refine
  # R-Studio
  # Jupyter Notebooks

# Audio tools:

  - name: whisper
    owner: bgruening
    tool_panel_section_label: Machine Learning

# Imaging tools:

    # E-Scriptorium -work in progress
    
  - name: graphicsmagick_image_convert
    owner: bgruening
    tool_panel_section_label: "Convert Formats"
    
   - name: imagej2_enhance_contrast
    owner: imgteam
    tool_panel_section_label: "Imaging"
  

# Visualisation tools:

  - name: wordcloud
    owner: bgruening
    tool_panel_section_label: Graph/Display Data

# Domain Specific:

  # GIS
  # OBO for ontologies

  - name: tabpfn
    owner: bgruening
    tool_panel_section_label: Machine Learning

# export to Zenodo  https://galaxyproject.org/news/2024-05-03-inveniordm-integration/
